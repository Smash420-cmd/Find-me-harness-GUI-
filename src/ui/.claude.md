# src/ui/ — THE WRAPPER (projection only)

Projection only. **No decision path.** No write path to the command bus. Render
the current run's results and nothing more. All logic lives behind the seam; the
wrapper shows what the engine emitted (Constitution Law 4). Current-run only.
