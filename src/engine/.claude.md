# src/engine/ — GENERAL HEMISPHERE

Domain-free. **Never import from `../chassis/`.** Speak only `Spec`, `Candidate`,
`VerifiedResult`, `ProofShot`. No `RAM`, no `price`, no `SKU`, no source name.
If you need a domain rule, you are in the wrong hemisphere — the chassis supplies
it through the mounting-point contract. Engine routes; it never invents rules.
