# src/engine/verify/ — THE TRUST FLOOR (human-PR only)

Tier **ROUTING** only — never domain rules. Route cheapest-first (0→3), apply
graceful degradation (tier fails ⇒ fall back + flag confidence; never silent
pass), and enforce the proof-shot gate: no `ProofShot` ⇒ no `VerifiedResult`.

Authority on what counts as *verified* = `src/chassis/ram/verify.ts` (the chassis
supplies each tier's rule). This module changes by human PR only (Constitution
Law 3). Test-first (Law 8): the red tests land before the implementation.
