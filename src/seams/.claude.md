# src/seams/ — EMPTY SCAFFOLD

Interfaces only. **Do not implement.** Each room is furnished only when its
benchmark in the promotion register (/.claude.md) is met. `providers/cache` is a
STUB governed by these rules too: it returns a miss until the verify-loop hits
its reliability bar.
