# src/chassis/ram/ — THE RAM DOMAIN AUTHORITY

Domain rules live HERE. This is the **only** place the words `SKU`, `price`,
`in stock`, `DDR4`, `DDR5`, capacity/speed/timing may appear. Supply the three
mounting-point pieces (spec schema, verify rules, rank) + the Umart source.
Never reach up into the engine; the engine consumes this contract, not vice versa.
